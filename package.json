{
    "scripts": {
        "test:chrome": "cypress run --browser chrome --no-exit",
        "test:console": "cypress run --headless --browser chrome"
    },
    "cypress-cucumber-preprocessor": {
        "step_definitions": "cypress/support/steps"
    },
    "devDependencies": {
        "cypress": "^4.11.0",
        "cypress-cucumber-preprocessor": "^2.5.4"
    }
}
